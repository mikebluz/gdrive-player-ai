node_modules/
.env
deploy.env
js/config.js
.DS_Store
**/.DS_Store
spike/dsp/target/
dsp/target/

# Mike's own scratch/test scripts, and the throwaway puppeteer runners used
# while building — never meant to be committed. _dialtest.mjs got in once via
# `git add -A`, which is exactly why that is not used here.
_*.mjs
_*.png
_*.html

# built desktop app — large, and rebuilt on demand
dist/
.desktop-stage/
